// Prod: front y back en orígenes separados. `apiBase` apunta al backend público.
// Ajustá estos valores al deployar (o generalos en build desde variables de entorno).
export const environment = {
  production: true,
  apiBase: 'https://api.padel-hub.com.ar',
  baseDomain: 'padel-hub.com.ar',
};
